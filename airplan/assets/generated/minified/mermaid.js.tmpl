function ce() {
  let t = 0;
  return {
    next: () => ++t,
    isCurrent: (n) => n === t
  };
}

var de = '<svg class="icon" aria-hidden="true" viewBox="0 0 16 16" fill="currentColor"><path d="M3.72 3.72a.75.75 0 0 1 1.06 0L8 6.94l3.22-3.22a.749.749 0 0 1 1.275.326.749.749 0 0 1-.215.734L9.06 8l3.22 3.22a.749.749 0 0 1-.326 1.275.749.749 0 0 1-.734-.215L8 9.06l-3.22 3.22a.751.751 0 0 1-1.042-.018.751.751 0 0 1-.018-1.042L6.94 8 3.72 4.78a.75.75 0 0 1 0-1.06Z"/></svg>', me = '<svg class="icon" aria-hidden="true" viewBox="0 0 16 16" fill="currentColor"><path d="M1.75 10a.75.75 0 0 1 .75.75v2.5c0 .138.112.25.25.25h2.5a.75.75 0 0 1 0 1.5h-2.5A1.75 1.75 0 0 1 1 13.25v-2.5a.75.75 0 0 1 .75-.75Zm12.5 0a.75.75 0 0 1 .75.75v2.5A1.75 1.75 0 0 1 13.25 15h-2.5a.75.75 0 0 1 0-1.5h2.5a.25.25 0 0 0 .25-.25v-2.5a.75.75 0 0 1 .75-.75ZM2.75 2.5a.25.25 0 0 0-.25.25v2.5a.75.75 0 0 1-1.5 0v-2.5C1 1.784 1.784 1 2.75 1h2.5a.75.75 0 0 1 0 1.5ZM10 1.75a.75.75 0 0 1 .75-.75h2.5c.966 0 1.75.784 1.75 1.75v2.5a.75.75 0 0 1-1.5 0v-2.5a.25.25 0 0 0-.25-.25h-2.5a.75.75 0 0 1-.75-.75Z"/></svg>';

var M = Array.from(document.querySelectorAll("pre.mermaid")), Me = {{.MermaidURL}}, X = M.map((t) => t.textContent), y = M.map(() => new Map), G = M.map(() => new Set), z = window.__AIRPLAN_THEME_CATALOG__, ue = window.__AIRPLAN_MERMAID_THEMES__, pe = matchMedia("print"), Z = "__airplan-print-github-light", xe = 0, K = null, T = window.__airplanThemeState?.theme ?? z?.defaultLight ?? "github-light", ge = M.map(() => T), S = pe.matches, W, U = new Set, he = ce();
function j(t, n, l = !0) {
  let o = y[t].get(n);
  if (!o)
    return !1;
  if (M[t].innerHTML = o.svg, o.bindFunctions)
    o.bindFunctions(M[t]);
  if (K)
    K.enhance(M[t]);
  if (l && n !== Z)
    ge[t] = n;
  return !0;
}
function I(t) {
  let n = !0;
  return y.forEach((l, o) => {
    if (!j(o, t))
      n = !1;
  }), n;
}
function J() {
  y.forEach((t, n) => {
    if (!j(n, T))
      j(n, ge[n]);
  });
}
function F(t, n = !0) {
  if (W)
    W(t, n);
  else
    U.add(t);
}
window.addEventListener("airplan:themechange", (t) => {
  let n = t.detail;
  if (T = n.theme, !S)
    F(n.theme);
});
window.addEventListener("airplan:themeprepare", (t) => {
  let n = t.detail;
  F(n.theme, !1);
});
pe.addEventListener("change", (t) => {
  if (S = t.matches, S)
    I(Z);
  else
    F(T), J();
});
window.addEventListener("beforeprint", () => {
  S = !0, I(Z);
});
window.addEventListener("afterprint", () => {
  S = !1, F(T), J();
});
function _(t, n, l) {
  let o = document.createElement("button");
  return o.type = "button", o.className = "mermaid-button", o.setAttribute(`data-airplan-mermaid-${n}`, ""), o.setAttribute("aria-label", t), o.title = t, o.innerHTML = l, o;
}
function we(t) {
  let n = t.getAttribute("viewBox")?.trim().split(/[\s,]+/).map(Number);
  if (n?.length === 4 && Number.isFinite(n[0]) && Number.isFinite(n[1]) && Number.isFinite(n[2]) && n[2] > 0 && Number.isFinite(n[3]) && n[3] > 0)
    return { x: n[0], y: n[1], width: n[2], height: n[3] };
  let l = Number.parseFloat(t.getAttribute("width") || ""), o = Number.parseFloat(t.getAttribute("height") || "");
  return {
    x: 0,
    y: 0,
    width: Number.isFinite(l) && l > 0 ? l : 800,
    height: Number.isFinite(o) && o > 0 ? o : 600
  };
}
function be(t) {
  let n = t.cloneNode(!0), l = `airplan-mermaid-viewer-${xe++}-`, o = new Map, x = [n, ...n.querySelectorAll("*")];
  x.forEach((m) => {
    let d = m.getAttribute("id");
    if (!d)
      return;
    let u = `${l}${d}`;
    o.set(d, u), m.setAttribute("id", u);
  }), x.forEach((m) => {
    Array.from(m.attributes).forEach((d) => {
      let u = d.value;
      if (o.forEach((s, a) => {
        if (u = u.replaceAll(`url(#${a})`, `url(#${s})`), u === `#${a}`)
          u = `#${s}`;
      }), d.name === "aria-labelledby" || d.name === "aria-describedby")
        u = u.split(/\s+/).map((s) => o.get(s) || s).join(" ");
      if (u !== d.value)
        m.setAttribute(d.name, u);
    });
  });
  let w = Array.from(o).sort(([m], [d]) => d.length - m.length);
  return n.querySelectorAll("style").forEach((m) => {
    let d = m.textContent || "";
    w.forEach(([u, s]) => {
      let a = u.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), g = new RegExp(`#${a}(?![\\w-])`, "g");
      d = d.replace(g, () => `#${s}`);
    }), m.textContent = d;
  }), n;
}
function N(t, n, l) {
  return Math.min(Math.max(t, n), l);
}
function ye() {
  let t = document.createElement("dialog");
  if (typeof t.showModal !== "function")
    return null;
  t.id = "airplan-mermaid-dialog", t.className = "mermaid-dialog", t.setAttribute("data-airplan-mermaid-dialog", ""), t.setAttribute("aria-labelledby", "airplan-mermaid-dialog-title");
  let n = document.createElement("div");
  n.className = "mermaid-dialog-shell";
  let l = document.createElement("header");
  l.className = "mermaid-dialog-header";
  let o = document.createElement("strong");
  o.id = "airplan-mermaid-dialog-title", o.textContent = "Mermaid diagram", l.appendChild(o);
  let x = document.createElement("div");
  x.className = "mermaid-toolbar", x.setAttribute("role", "toolbar"), x.setAttribute("aria-label", "Diagram zoom controls");
  let w = _("Zoom out", "zoom-out", "&minus;"), m = document.createElement("output");
  m.className = "mermaid-zoom-value", m.setAttribute("data-airplan-mermaid-zoom-value", ""), m.setAttribute("aria-live", "polite"), m.textContent = "100%";
  let d = _("Zoom in", "zoom-in", "+"), u = _("Fit diagram", "fit", "Fit"), s = _("Close diagram viewer", "close", de);
  x.append(w, m, d, u, s), l.appendChild(x);
  let a = document.createElement("div");
  a.className = "mermaid-canvas", a.setAttribute("data-airplan-mermaid-canvas", ""), a.tabIndex = 0, a.setAttribute("role", "group"), a.setAttribute("aria-label", "Zoomable Mermaid diagram. Scroll, pinch, or use plus and minus to zoom. Drag or use arrow keys to pan.");
  let g = document.createElement("div");
  g.className = "mermaid-surface", g.setAttribute("data-airplan-mermaid-surface", ""), a.appendChild(g);
  let V = document.createElement("p");
  V.className = "mermaid-help", V.textContent = "Scroll or pinch to zoom · Drag to pan · 0 to fit", n.append(l, a, V), t.appendChild(n), document.body.appendChild(t);
  let L = null, h = null, C = null, f = { x: 0, y: 0, width: 1, height: 1 }, c = 1, B = 0, R = 0, v = new Map, Q = 0, ee = 0, te = 0, ae = 0, k = 0, q = 0, Y = 0;
  function A() {
    if (m.textContent = `${Math.round(c * 100)}%`, !C)
      return;
    let e = a.getBoundingClientRect(), i = Math.max(e.width, 1) / c, r = Math.max(e.height, 1) / c, p = f.x + f.width / 2 + B, b = f.y + f.height / 2 + R;
    C.setAttribute("viewBox", [p - i / 2, b - r / 2, i, r].join(" "));
  }
  function H() {
    let e = a.getBoundingClientRect(), i = Math.max(e.width - 48, 1), r = Math.max(e.height - 48, 1);
    c = N(Math.min(i / f.width, r / f.height, 1), 0.05, 8), B = 0, R = 0, A();
  }
  function ne(e, i, r, p, b) {
    let D = N(e, 0.05, 8), E = a.getBoundingClientRect(), se = E.left + E.width / 2, le = E.top + E.height / 2;
    B += (i - se) / c - (p - se) / D, R += (r - le) / c - (b - le) / D, c = D, A();
  }
  function P(e, i, r) {
    if (i !== void 0 && r !== void 0) {
      ne(e, i, r, i, r);
      return;
    }
    let p = N(e, 0.05, 8);
    if (p === c)
      return;
    c = p, A();
  }
  function re(e) {
    Q = e.x, ee = e.y, te = B, ae = R;
  }
  function fe() {
    let [e, i] = Array.from(v.values());
    if (!e || !i) {
      k = 0;
      return;
    }
    k = Math.hypot(i.x - e.x, i.y - e.y), q = (e.x + i.x) / 2, Y = (e.y + i.y) / 2;
  }
  function ie(e, i) {
    let r = be(e);
    if (C = r, f = we(e), r.style.width = "100%", r.style.height = "100%", r.style.maxWidth = "none", r.style.maxHeight = "none", g.replaceChildren(r), i)
      H();
    else
      A();
  }
  if (typeof ResizeObserver === "function")
    new ResizeObserver(() => A()).observe(a);
  else
    window.addEventListener("resize", A);
  function O() {
    if (t.open)
      t.close();
  }
  function ve(e, i, r) {
    let p = e.querySelector("svg");
    if (!p)
      return;
    L = e, h = i, ie(p, !1), t.showModal(), document.body.classList.add("mermaid-dialog-open"), H(), a.classList.toggle("mermaid-canvas-pointer-focus", r), a.focus();
  }
  w.addEventListener("click", () => P(c / 1.25)), d.addEventListener("click", () => P(c * 1.25)), u.addEventListener("click", H), s.addEventListener("click", O), t.addEventListener("cancel", (e) => {
    e.preventDefault(), O();
  }), t.addEventListener("click", (e) => {
    if (e.target === t)
      O();
  }), t.addEventListener("close", () => {
    document.body.classList.remove("mermaid-dialog-open"), v.clear(), a.classList.remove("mermaid-canvas-panning", "mermaid-canvas-pointer-focus");
    let e = h;
    if (L = null, h = null, e?.isConnected)
      setTimeout(() => e.focus(), 0);
  }), t.addEventListener("keydown", (e) => {
    if (a.classList.remove("mermaid-canvas-pointer-focus"), e.ctrlKey || e.metaKey || e.altKey)
      return;
    if (e.key === "+" || e.key === "=") {
      e.preventDefault(), P(c * 1.25);
      return;
    }
    if (e.key === "-") {
      e.preventDefault(), P(c / 1.25);
      return;
    }
    if (e.key === "0") {
      e.preventDefault(), H();
      return;
    }
    if (e.target !== a)
      return;
    let i = e.shiftKey ? 120 : 40, r = {
      ArrowLeft: [-i, 0],
      ArrowRight: [i, 0],
      ArrowUp: [0, -i],
      ArrowDown: [0, i]
    }[e.key];
    if (!r)
      return;
    e.preventDefault(), B += r[0] / c, R += r[1] / c, A();
  }), a.addEventListener("wheel", (e) => {
    e.preventDefault();
    let i = e.deltaMode === 1 ? 16 : e.deltaMode === 2 ? a.clientHeight : 1, r = N(e.deltaY * i, -500, 500);
    P(c * Math.exp(-r * 0.001), e.clientX, e.clientY);
  }, { passive: !1 }), a.addEventListener("pointerdown", (e) => {
    if (e.button !== 0 || v.has(e.pointerId) || v.size >= 2)
      return;
    v.set(e.pointerId, { x: e.clientX, y: e.clientY }), a.classList.add("mermaid-canvas-panning"), a.classList.add("mermaid-canvas-pointer-focus");
    try {
      a.setPointerCapture(e.pointerId);
    } catch {}
    if (v.size === 1)
      re({ x: e.clientX, y: e.clientY });
    else
      fe();
  }), a.addEventListener("pointermove", (e) => {
    let i = v.get(e.pointerId);
    if (!i)
      return;
    if (i.x = e.clientX, i.y = e.clientY, v.size === 1) {
      B = te - (e.clientX - Q) / c, R = ae - (e.clientY - ee) / c, A();
      return;
    }
    let [r, p] = Array.from(v.values());
    if (!r || !p)
      return;
    let b = Math.hypot(p.x - r.x, p.y - r.y), D = (r.x + p.x) / 2, E = (r.y + p.y) / 2;
    if (k > 0 && b > 0)
      ne(c * (b / k), q, Y, D, E);
    k = b, q = D, Y = E;
  });
  function oe(e) {
    if (!v.has(e.pointerId))
      return;
    try {
      a.releasePointerCapture(e.pointerId);
    } catch {}
    if (v.delete(e.pointerId), v.size === 0) {
      k = 0, a.classList.remove("mermaid-canvas-panning");
      return;
    }
    let i = v.values().next().value;
    if (i)
      re(i);
  }
  return a.addEventListener("pointerup", oe), a.addEventListener("pointercancel", oe), {
    enhance(e) {
      let i = e.querySelector("svg");
      if (!i)
        return;
      let r = e.querySelector(":scope > [data-airplan-mermaid-open]");
      if (!r) {
        r = _("Open diagram viewer", "open", me), r.classList.add("mermaid-open"), r.setAttribute("aria-haspopup", "dialog"), r.setAttribute("aria-controls", t.id);
        let p = r;
        r.addEventListener("click", (b) => ve(e, p, b.detail > 0)), e.appendChild(r);
      }
      if (L === e && t.open)
        h = r, ie(i, !1);
    }
  };
}
try {
  let x = function(s) {
    return y.every((a, g) => a.has(s) || G[g].has(s));
  }, w = function(s, a = s) {
    if (x(a))
      return Promise.resolve();
    let g = o.get(a);
    if (g)
      return g;
    let V = a === Z ? t.print : t.themes[s];
    if (!V)
      return Promise.resolve();
    let L = l.then(async () => {
      if (x(a))
        return;
      try {
        n.initialize({
          startOnLoad: !1,
          securityLevel: "strict",
          secure: ["theme", "themeVariables", "themeCSS", "darkMode"],
          theme: "base",
          themeVariables: V
        });
      } catch (h) {
        G.forEach((C, f) => {
          if (C.add(a), y[f].size === 0)
            M[f].textContent = X[f], M[f].classList.add("mermaid-failed");
        }), console.warn(`airplan: Mermaid ${s} initialization failed`, h);
        return;
      }
      for (let [h, C] of X.entries()) {
        if (y[h].has(a) || G[h].has(a))
          continue;
        try {
          let f = await n.render(`airplan-mermaid-${a.replace(/[^a-z0-9-]/g, "-")}-${h}`, C);
          y[h].set(a, f);
          let c = M[h];
          if (c.classList.add("mermaid-rendered"), c.classList.remove("mermaid-failed"), !K)
            K = ye();
        } catch (f) {
          if (G[h].add(a), y[h].size === 0)
            M[h].textContent = C, M[h].classList.add("mermaid-failed");
          console.warn(`airplan: Mermaid ${s} diagram ${h + 1} rendering failed`, f);
        }
      }
    });
    return l = L.catch(() => {}), o.set(a, L), L.then(() => o.delete(a), () => o.delete(a)), L;
  };
  if (!z || !ue)
    throw Error("theme catalog is unavailable");
  let t = ue, { default: n } = await import(Me), l = Promise.resolve(), o = new Map;
  async function m(s, a = !0) {
    let g = a ? he.next() : 0;
    if (await w(s), a && he.isCurrent(g) && T === s && !S)
      I(s);
  }
  W = m;
  let d = window.__airplanThemeState, u = new Set([
    d?.lightTheme ?? z.defaultLight,
    d?.darkTheme ?? z.defaultDark
  ]);
  for (let s of u)
    await w(s);
  if (await o.get("github-light"), t.themes["github-light"] && x("github-light"))
    y.forEach((s, a) => {
      let g = s.get("github-light");
      if (g)
        s.set(Z, g);
      if (G[a].has("github-light"))
        G[a].add(Z);
    });
  else
    await w("github-light", Z);
  for (let s of U)
    await w(s);
  if (U.clear(), await w(T), S)
    I(Z);
  else
    J();
} catch (t) {
  M.forEach((n, l) => {
    n.textContent = X[l], n.classList.add("mermaid-failed");
  }), console.warn("airplan: Mermaid rendering failed", t);
}
