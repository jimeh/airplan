---
title: Bundle overview
---

# Bundle overview

This entry page links to the [design notes](docs/design.md#deep-dive) and ships
with a source-code page.

<script>
window.__airplanBundleLifetime = crypto.randomUUID();
sessionStorage.setItem(
  "airplan-bundle-authored-runs",
  String(Number(sessionStorage.getItem("airplan-bundle-authored-runs") || "0") + 1),
);
</script>

## Entry details

The entry has no Mermaid diagram. The design page does.

## Navigation

The Pages rail links to every managed page.
